// hook into opencode's beforeToolUse(tool, args) event
// we need to intercept the plugin here so in each tool call
// it req to mcp gateway and check via our check() based on the workflow defined
// in dashboard
